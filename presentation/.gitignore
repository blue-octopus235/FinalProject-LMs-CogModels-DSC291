# LaTeX / Beamer build artifacts (regenerated by `make`)
*.aux
*.log
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
*.dvi
*.bcf
*.run.xml
*.nav
*.snm
*.vrb

# Note: main.pdf is intentionally NOT ignored so the built slides can be
# shared/committed. Run `make clean` to remove the artifacts above.
