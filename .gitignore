# large data files (share via Drive/Dropbox, not git)
data/*.tsv
data/*.tsv.gz
data/*.csv
291-data/
# checkpoints & vocab (regenerate via training)
checkpoints/*.pt
# results are regenerated; keep figures if you want them in the repo
# (uncomment to ignore) results/*.png
# compiled report PDF is a build artifact (regenerate with `make` in report/);
# tracking it causes binary merge conflicts on every rebuild
report/main.pdf
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/
.antigravitycli/
.claude/
