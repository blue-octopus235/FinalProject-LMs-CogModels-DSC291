# LaTeX build artifacts (regenerated by `make`)
*.aux
*.log
*.out
*.bbl
*.blg
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
*.dvi
*.bcf
*.run.xml
*.nav
*.snm
*.vrb

# The compiled PDF is a build artifact: tracking it caused a binary merge
# conflict on every rebuild. Regenerate with `make`; share the PDF out-of-band.
main.pdf

# Run `make clean` to remove the artifacts above.
