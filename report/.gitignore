# LaTeX build artifacts (regenerated by `make`)
*.aux
*.log
*.out
*.bbl
*.blg
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
*.dvi
*.bcf
*.run.xml
*.nav
*.snm
*.vrb

# Note: main.pdf is intentionally NOT ignored so the built report can be
# shared/committed. Run `make clean` to remove the artifacts above.
